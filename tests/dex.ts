import { expect } from 'chai';
import { Dex } from '../src/dex';

describe('Prices', () => {
  it("should return price", () => {
    // let dex = new Dex();
    expect(true).to.equal(true);
  });
});
